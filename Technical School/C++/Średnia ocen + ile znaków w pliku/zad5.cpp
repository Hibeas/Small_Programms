#include <cstdlib>
#include <iostream>
#include<fstream>
using namespace std;

int main(int argc, char *argv[])
{
    ifstream plik1;
    ifstream plik2;
    plik.open("//plik1.txt");
    plik.open("//plik2.txt");
    int count;
    int count2;
    for(count=0;plik.get()!=EOF;++count){}
    for(count2=0;plik.get()!=EOF;++count2){}
    system("PAUSE");
    return EXIT_SUCCESS;
}
